mod.wizards.newContentElement {
  wizardItems {
    content.elements.newsletterregister {
      iconIdentifier = content-NewsletterRegister
      title = Newsletter Registrierungsformular [NewsletterRegister]
      description = Mit diesem Element kann das Newsletter Registrierungsformular eingefügt werden.
      tt_content_defValues.CType = newsletterregister
    }
    content.show := addToList(newsletterregister)
  }
}
